#
# Format:
#
# <stage name>:
#   app: <Heroku app name>
#   stack: <Heroku stack, optional>
#   tag: <git tag pattern, optional>
#   repo: <git repository, optional>
#   config:
#     - <Heroku config:var name>: <Heroku config:var value>
#

staging:
  stack: cedar
  app: sony-phoenix-staging
  config: &default
    BUNDLE_WITHOUT: "development:test"
  addons:
    - logging:basic
    - scheduler:standard
