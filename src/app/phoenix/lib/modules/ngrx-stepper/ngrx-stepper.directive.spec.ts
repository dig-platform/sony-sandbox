import { NgrxStepperDirective } from './ngrx-stepper.directive';

describe('NgrxStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new NgrxStepperDirective();
    expect(directive).toBeTruthy();
  });
});
