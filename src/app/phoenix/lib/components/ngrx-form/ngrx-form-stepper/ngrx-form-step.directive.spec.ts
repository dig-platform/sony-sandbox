import { NgrxFormStepDirective } from './ngrx-form-step.directive';

describe('NgrxFormStepDirective', () => {
  it('should create an instance', () => {
    const directive = new NgrxFormStepDirective();
    expect(directive).toBeTruthy();
  });
});
