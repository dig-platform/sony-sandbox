import { FormStepperStepDirective } from './form-stepper-step.directive';

describe('FormStepperStepDirective', () => {
  it('should create an instance', () => {
    const directive = new FormStepperStepDirective();
    expect(directive).toBeTruthy();
  });
});
