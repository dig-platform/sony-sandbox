import { AccountRequestStepDirective } from './account-request-step.directive';

describe('AccountRequestStepDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountRequestStepDirective();
    expect(directive).toBeTruthy();
  });
});
